export const sizeRowGrid = 10;
export const sizeColumnGrid = 10;
export const canvasWidth = Math.floor((window.screen.width * 2 / 3) / sizeColumnGrid) * sizeColumnGrid;
export const canvasHeight = Math.floor((window.screen.height / 2) / sizeRowGrid) * sizeRowGrid;
export const colorFocus = 30;