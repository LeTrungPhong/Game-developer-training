export const sizeRow = 10;
export const sizeColumn = 10;
export const canvasWidth = Math.floor(window.screen.width * 2 / 3 / sizeColumn) * sizeColumn;
export const canvasHeight = Math.floor(window.screen.height * 2 / 3 / sizeRow) * sizeRow;